/* let newArray = [];
tagsArray.map((tag) => {
  newArray.push(tag);
});
console.log(typeof newArray);
 */

names = ["Sagar", "Shreya", "Dipesh", "Nista"];
console.log(names[0]);

names.push("Rocky");
console.log(names);

names.pop();
console.log(names);

names.unshift("Pizza"); //adds to first
console.log(names);

console.log(names.indexOf("Shreya"));
